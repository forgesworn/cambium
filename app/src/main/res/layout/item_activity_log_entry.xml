<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/logTimestampValue"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            android:fontFamily="monospace"
            android:textColor="@color/on_background_muted"
            android:textSize="11sp"
            tools:text="2026-07-10 09:14:02" />

        <TextView
            android:id="@+id/logOutcomeValue"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:fontFamily="monospace"
            android:gravity="end"
            android:textColor="@color/on_background"
            android:textSize="11sp"
            tools:text="Signed" />
    </LinearLayout>

    <TextView
        android:id="@+id/logAppValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textColor="@color/on_background"
        tools:text="Amethyst" />

    <TextView
        android:id="@+id/logMethodValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textColor="@color/on_background_muted"
        android:textSize="12sp"
        tools:text="sign_event (kind 1)" />

    <TextView
        android:id="@+id/logIdentityValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textColor="@color/on_background_muted"
        android:textSize="12sp"
        tools:text="Living room Heartwood" />

</LinearLayout>
